/**
 * @packageDocumentation
 * @module Utility
 */
import { RGBATuple } from '../types/colour';
/**
 * Takes an input string of rgba(num, num, num, num) and converts it to a hex
 * @param input The input RGBA
 * @private
 * @internal
 * @returns A tuple containing the
 */
export declare function convertRGBAStrToHexComponents(input: string): RGBATuple;
/**
 * @private
 * @internal
 * @param hex
 * @param alpha
 */
export declare function convertHexToRGBA(hex: string, alpha?: number): string;
